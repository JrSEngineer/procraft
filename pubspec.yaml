name: procraft
description: "A new Flutter project."

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.2.3 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  flutter_modular: ^6.3.3
  dio: ^5.7.0
  firebase_auth: ^4.17.8
  firebase_core: ^2.27.0
  fl_chart: ^0.69.0
  cloud_firestore: ^4.15.8
  shared_preferences: ^2.2.3

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/procraft_logo.png
